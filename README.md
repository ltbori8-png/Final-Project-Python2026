# Final-Project-Python2026
# App Title: Price Saver
# Upon hitting the play button, just wait as the code will run until it runs the full code.
# The only known bugs that are appearing without the code breaking is that the span tags appear when results are collected, when attempting to fix this, the code always crashes and it doesn't work. While this is unavoidable without the code crashing, it still works perfectly fine, so it's okay if you ignore it, because I don't know how to remove that without my code having a problem. 
# I worked alone on this project, so I was responsible for the entireity of this code. The concepts I used for this were JSON File Read and Write, they appeared upon creating the file before the webscraping code. OOP 'Classes', used to define the functions that my code will look for as the code runs. Webscraping, used to collect data on the prices and websites that sell the chosen product to compare prices.
